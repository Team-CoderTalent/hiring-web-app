{
  "name": "hiring-web-app-main",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "server": "cd backend && NODE_ENV=developemnt yarn dev",
    "client": "cd client && yarn start",
    "dev": "concurrently \"yarn run server\" \"yarn run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "CoderTalent",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0",
    "nodemon": "^2.0.3"
  }
}
